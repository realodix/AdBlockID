{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Fix",
            "type": "shell",
            "command": "./vendor/bin/hippo fix",
            "problemMatcher": []
        },
        {
            "label": "Build",
            "type": "shell",
            "command": "./vendor/bin/hippo build",
            "problemMatcher": []
        },
        {
            "label": "Soft Build",
            "type": "shell",
            "command": "./vendor/bin/hippo fix && ./vendor/bin/hippo build --skip-version",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
